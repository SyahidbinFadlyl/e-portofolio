import { Box } from "@mui/material";

export default function Content() {
  return (
    <Box
      sx={{
        py: { sm: "6rem", xs: "3rem" },
        width: "52%",
      }}
    >
      test
    </Box>
  );
}
